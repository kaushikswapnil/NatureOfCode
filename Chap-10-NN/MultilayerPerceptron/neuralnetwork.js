
class NeuralNetwork
{
	constructor(numInputNodes, numHiddenNodes, numOutputNodes)
	{
		this.m_NumInputNodes = numInputNodes;
		this.m_NumHiddenNodes = nimHiddenNodes;
		this.m_NumOutputNodes = numOutputNodes;
	}
}
